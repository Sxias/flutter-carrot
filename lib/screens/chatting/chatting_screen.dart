import 'package:flutter/material.dart';

class ChattingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("Chatting Screen build");
    return Scaffold(
      body: Center(child: Text("Chatting Screen")),
    );
  }

  const ChattingScreen();
}
