import 'package:flutter/material.dart';

class NearMeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("NearMe Screen build");
    return Scaffold(
      body: Center(child: Text("NearMe Screen")),
    );
  }

  const NearMeScreen();
}
